---
name: workflow-project-scoping
description: Define project scope, deliverables, timeline, budget, and risks to create a shared, signed-off understanding between agency and client. Use when starting a project or clarifying an ambiguous brief.
---

# Project Scoping

Define the boundaries and contents of a project clearly enough that both agency and client know exactly what is included, what is not, and what will be delivered.

## What You Do

You help turn vague briefs into precise project definitions that prevent scope creep, missed expectations, and billing disputes.

## Scope Document Components

- **Project objective**: One sentence on what the project must achieve
- **In scope**: Explicit list of what is included — phases, deliverables, platforms, languages, user types
- **Out of scope**: Explicit list of what is excluded — just as important as what's included
- **Deliverables**: Itemised list with format and acceptance criteria for each
- **Milestones**: Key review and sign-off points with dates
- **Assumptions**: What you are assuming to be true — if an assumption proves false, scope may change
- **Dependencies**: What the agency needs from the client, and when, to meet the timeline
- **Revision rounds**: Number of revision rounds included per deliverable, and the feedback management approach (see the `workflow-feedback-management` skill for the review structure and change-request process)
- **Change request process**: How out-of-scope requests will be handled and priced

## Risk Log

Identify the top 3–5 scope risks and the mitigation for each — this surfaces where the project is most likely to slip or expand before it happens, not after.

## Client Sign-Off

Produce a scope summary formatted specifically for client review and sign-off, distinct from the internal working scope document. A signed scope document is the agency's most important legal protection.

## Scope Creep Prevention

- Scope creep usually starts with a small "quick addition" — document every change request in writing
- Review assumptions at the project midpoint
- A signed scope document is the agency's most important legal protection

## Timeline Principles

- Build in buffer — assume client reviews take twice as long as expected
- Identify the critical path and flag which delays have downstream consequences
- Distinguish between client-dependent milestones and agency-only milestones

## Output

Signed-off scope document including deliverables, in/out of scope, milestone schedule, assumptions, revision rounds, feedback process, and risk log.

Consider following up with `deliverables-proposal-writing` to formalise the scope into a deliverable proposal, or `workflow-design-sprint-plan` to translate scope into sprint planning.

## Build

Use the `figma-use` skill to materialize this as a new frame on the current page in the active Figma file, named `workflow-project-scoping — [input]`. Lay it out as a polished one-page document: an eyebrow label, bold headline, and meta line at the top; numbered sections with clear typographic hierarchy; tables for structured or comparative data; and colored status badges or callout boxes to highlight key points, severity, or priority where relevant. Build incrementally and verify the result with a screenshot before finishing.
